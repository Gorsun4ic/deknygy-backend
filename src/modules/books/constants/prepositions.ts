// Words considered too short or common (e.g., prepositions) to be useful
// for grouping if a title isn't a volume. Used to create a more 'core' title key.
export const PREPOSITIONS = new Set(['у', 'в', 'на', 'a', 'an', 'the']);
