{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*spec.ts",
    "prisma", // Exclude the folder with seeds
    "prisma.config.ts" // Exclude the config file from compilation
  ]
}
