export const NUM_USERS = Number(process.env.NUM_USERS) || 16000;
export const NUM_QUERIES = Number(process.env.NUM_QUERIES) || 8000;
export const NUM_BOOKS = Number(process.env.NUM_BOOKS) || 100000;
export const NUM_SEARCH_LOGS = Number(process.env.NUM_SEARCH_LOGS) || 50000;
